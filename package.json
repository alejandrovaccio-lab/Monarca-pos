{
  "name": "monarca-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Monarca POS — sistema multi-sucursal y multitarea para Mercadito Esquina",
  "scripts": {
    "prisma:format": "prisma format",
    "prisma:validate": "prisma validate",
    "prisma:generate": "prisma generate",
    "password:hash": "tsx scripts/hash-password.ts",
    "validate": "prisma validate && prisma generate && tsc --noEmit",
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "@node-rs/argon2": "^2.0.2"
  },
  "devDependencies": {
    "prisma": "^6.15.0",
    "typescript": "^5.9.2",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3"
  },
  "engines": {
    "node": ">=22"
  }
}
