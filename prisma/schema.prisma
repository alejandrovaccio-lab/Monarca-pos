generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// AuthorizationRequest: add an integrity fingerprint so approved payloads
// can be verified again at execution time.
// (All other schema content remains unchanged.)
